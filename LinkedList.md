# LinkedList

It is a Linked representation of List implementation class.
-It allows duplicate elements because it implemnts List interface.
-It allows null value also.
-It occupies more memory because data stored in nodes.
-
